import React from 'react';

const ButtonPrimary = ({ buttonName }) => {
    return (
        <button className='btn bg-[#FACC15] text-[#15803D] rounded-full border-0'>{buttonName}</button>
    );
};

export default ButtonPrimary;